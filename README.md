# Youtube_data_ETL
This project deals with building a simple UI with streamlit, retrieving data from the Youtube API, storing it in a MongoDB datalake, migrating it to a SQL data warehouse(using Studio 3T), querying the data warehouse with SQL, and displaying  the data in the Streamlit App. 
